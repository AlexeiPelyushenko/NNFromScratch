# NNFromScratch
Neural Networks made from scratch. Numpy is allowed, but no pytorch/huggingface/sklearn (besides downloading datasets), etc.

Currently capabilities:
- Solve XOR (MSE, Cross Entropy)
- Linear regression through feed forward neural network
